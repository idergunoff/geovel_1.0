# План разработки экспериментального блока ML Clutter для очистки воздушных помех

## 1. Назначение документа

Документ описывает поэтапный план разработки экспериментального блока `ML Clutter` для исследования ML-очистки георадарных профилей от воздушных помех. Экспериментальный блок должен запускаться из главного окна приложения по кнопке `ML Clutter`, открывать отдельное окно управления экспериментом, позволять выбирать текущий профиль, генерировать синтетические помехи, использовать реальные зашумлённые радарограммы как паттерны шума, обучать модель очистки и визуализировать все ключевые преобразования в основном окне `MainWindow`.

Основная исследовательская идея:

1. Берём чистую радарограмму `X_clean` в амплитудном диапазоне `0..256`.
2. Генерируем или извлекаем шумовой паттерн `N_noise` в том же диапазоне; по умолчанию извлечение паттерна сохраняет исходную шкалу амплитуд, без standard-нормализации.
3. Пользователь выбирает режим наложения real-pattern шума: основной режим — доминирующее амплитудное перекрытие, дополнительный legacy-режим — additive.
4. Для доминирующего перекрытия переводим `X_clean` и `N_noise` в диапазон `-128..+128`, в каждой точке выбираем значение с большим модулем и возвращаем результат в `0..256`.
5. Обучаем модель по основной паре `input: X_noisy`, `target: X_clean`; clutter/mask остаются диагностическими артефактами, а не обязательными target для baseline-обучения.
6. Применяем модель к синтетическим и реальным профилям.
7. Оцениваем, насколько хорошо модель удаляет воздушную помеху и насколько бережно сохраняет полезные подповерхностные отражения.

Ключевое расширение относительно первоначальной схемы: генератор должен поддерживать не только аналитические синтетические помехи, но и реальные радарограммы с шумом, загружаемые как библиотека паттернов воздушных помех. Это нужно, чтобы уменьшить риск расхождения между синтетическими и реальными помехами.

---

## 2. Общая архитектура блока

Экспериментальный блок состоит из трёх уровней:

1. UI-уровень.
2. Экспериментальная ML-логика.
3. Хранилище артефактов эксперимента.

### 2.1. UI-уровень

Планируемые компоненты:

- кнопка `ML Clutter` в главном окне;
- отдельное окно `ML Clutter Experiment`;
- вкладки настройки данных, генератора, паттернов шума, датасета, модели, обучения и инференса;
- передача результатов визуализации в основное окно `MainWindow`.

Окно эксперимента является панелью управления. Основное окно приложения используется как панель визуализации:

- основная радарограмма — отображение `clean`, `noise pattern`, `synthetic clutter`, `noisy`, `predicted clutter`, `cleaned`, `mask`, `error map`;
- график сигнала — сравнение отдельных трасс;
- график метрик — loss, SNR, RMSE, энергия вычитания;
- информационная панель — параметры эксперимента, логи, предупреждения и итоговые метрики.

### 2.2. ML-уровень

Планируемый пакет:

```text
ml_air_clutter/
    __init__.py
    config.py
    profile_io.py
    preprocessing.py
    synthetic_clutter.py
    noise_patterns.py
    pattern_library.py
    dataset.py
    model.py
    train.py
    inference.py
    metrics.py
    visualization.py
    experiment_runner.py
```

Назначение модулей:

| Модуль | Назначение |
|---|---|
| `config.py` | dataclass-конфиги генератора, датасета, обучения, инференса |
| `profile_io.py` | загрузка, валидация и экспорт профилей |
| `preprocessing.py` | нормализация, клиппинг, envelope, градиенты |
| `synthetic_clutter.py` | аналитическая генерация гипербол, линий, ringing, всплесков |
| `noise_patterns.py` | извлечение шумовых паттернов из реальных зашумлённых радарограмм |
| `pattern_library.py` | хранение, каталогизация и выбор паттернов шума |
| `dataset.py` | генерация обучающих пар и patch dataset |
| `model.py` | ML-модели очистки: baseline CNN, DnCNN, U-Net |
| `train.py` | цикл обучения, валидация, checkpoints |
| `inference.py` | применение модели к patch и полному профилю |
| `metrics.py` | MAE, RMSE, SNR, false subtraction, mask metrics |
| `visualization.py` | подготовка массивов и графиков для UI |
| `experiment_runner.py` | связующий слой между UI и ML-процессами |

### 2.3. Хранилище артефактов

Рекомендуемая структура:

```text
experiments/
    ml_clutter/
        YYYY-MM-DD_HH-MM-SS_experiment_name/
            config.json
            generator_config.json
            pattern_library_index.json
            dataset_summary.json
            train_log.csv
            metrics.json
            model_best.pt
            model_last.pt
            previews/
                clean.npy
                noisy.npy
                clutter.npy
                predicted_clutter.npy
                cleaned.npy
                mask.npy
            reports/
                report.md
```

Результатом каждого запуска должен быть самодостаточный каталог эксперимента, по которому можно воспроизвести генерацию, обучение и инференс.

---

## 3. Основные режимы работы блока

Блок `ML Clutter` должен поддерживать четыре режима.

### 3.1. Режим A: аналитическая синтетика

Чистый профиль зашумляется процедурно сгенерированными помехами:

- гиперболами;
- наклонными событиями;
- ringing;
- вертикальными всплесками;
- широкими зонами повышенной амплитуды;
- комбинациями нескольких типов.

Результат режима:

```text
X_clean + N_synthetic -> X_noisy
```

### 3.2. Режим B: реальные шумовые паттерны

Пользователь загружает реальные зашумлённые радарограммы. Из них извлекаются паттерны воздушных помех, которые затем накладываются на чистые профили.

Результат режима:

```text
X_clean + N_pattern_real -> X_noisy
```

### 3.3. Режим C: смешанный генератор

Генератор комбинирует аналитические помехи и реальные паттерны:

```text
N_clutter = a * N_synthetic + b * N_pattern_real
```

Этот режим должен стать основным для обучения, потому что он уменьшает synthetic-to-real gap.

### 3.4. Режим D: инференс на реальном профиле

Уже обученная модель применяется к профилю без ground truth:

```text
X_real_noisy -> model -> N_pred -> X_clean_pred
```

Результат оценивается визуально и через косвенные метрики: энергия вычитания, локальный RMS, карта уверенности, корреляция до/после.

---

## 4. Этап 1. Проектирование UI и точки входа `ML Clutter`

### 4.1. Задачи этапа

1. Добавить в главное окно кнопку `ML Clutter`.
2. Реализовать обработчик открытия окна эксперимента.
3. Создать форму `ML Clutter Experiment`.
4. Спроектировать вкладки окна.
5. Подготовить интерфейс обмена данными между окном эксперимента и `MainWindow`.

### 4.2. Предлагаемые вкладки окна

```text
1. Profiles
2. Noise Patterns
3. Generator
4. Dataset
5. Model
6. Training
7. Inference
8. Results
```

### 4.3. Вкладка `Profiles`

Назначение: выбор чистого профиля и профиля для инференса.

Элементы:

- список доступных профилей;
- кнопка `Refresh`;
- кнопка `Use Current Profile`;
- кнопка `Load as Clean`;
- кнопка `Load as Real Noisy`;
- блок статистики профиля.

Проверки:

- профиль должен быть двумерным массивом;
- ожидаемая форма: `[num_traces, 512]`;
- не должно быть `NaN` и `inf`;
- амплитуды должны быть конечными;
- должен быть понятен порядок осей.

### 4.4. Вкладка `Noise Patterns`

Назначение: загрузка и подготовка реальных шумовых паттернов.

Элементы:

- кнопка `Add Noisy Radarogram`;
- список загруженных шумовых профилей;
- настройки извлечения паттернов;
- кнопка `Extract Patterns`;
- кнопка `Preview Pattern`;
- кнопка `Save Pattern Library`;
- кнопка `Load Pattern Library`.

### 4.5. Вкладка `Generator`

Назначение: настройка аналитического и смешанного генератора.

Элементы:

- режим генерации: `Synthetic`, `Pattern`, `Mixed`;
- вероятность использования реального паттерна;
- сила аналитической помехи;
- сила паттерна;
- target SNR;
- seed;
- кнопки просмотра `Clean`, `Synthetic`, `Pattern`, `Mixed Clutter`, `Noisy`, `Mask`.

### 4.6. Вкладка `Dataset`

Назначение: создание обучающих patch-пар.

Элементы:

- patch width;
- stride;
- количество synthetic variants per profile;
- split train/validation/test;
- кнопка `Build Dataset`;
- кнопка `Preview Random Patch`;
- кнопка `Save Dataset`.

### 4.7. Вкладка `Model`

Назначение: выбор архитектуры и режима предсказания.

Режимы:

- `Predict Clean`;
- `Predict Clutter Residual`;
- `Predict Clutter + Mask`.

Режим по умолчанию:

```text
Predict Clutter Residual
```

### 4.8. Вкладка `Training`

Назначение: запуск обучения.

Элементы:

- epochs;
- batch size;
- learning rate;
- device;
- кнопка `Start Training`;
- кнопка `Stop`;
- progress bar;
- таблица метрик;
- статус текущей эпохи.

### 4.9. Вкладка `Inference`

Назначение: применение модели.

Элементы:

- выбор модели;
- выбор профиля;
- alpha-слайдер силы очистки;
- чекбокс mask-aware очистки;
- кнопка `Run Inference`;
- кнопка `Save Cleaned Profile`.

### 4.10. Ожидаемый результат этапа

После завершения этапа пользователь видит в главном окне кнопку `ML Clutter`. При нажатии открывается отдельное окно эксперимента. Окно ещё может не выполнять ML-обучение, но уже содержит финальную структуру вкладок, базовые элементы управления и заглушки обработчиков. Визуализационный интерфейс уже должен позволять отправлять массивы и текстовые сообщения из окна эксперимента в области `MainWindow`.

---

## 5. Этап 2. Загрузка и валидация профилей

### 5.1. Задачи этапа

1. Реализовать выбор текущего профиля из приложения.
2. Реализовать загрузку профиля как `clean`.
3. Реализовать загрузку профиля как `real noisy`.
4. Добавить валидацию формы массива.
5. Добавить расчёт базовой статистики.
6. Добавить отображение профиля в главном окне.

### 5.2. Валидация

Минимальные проверки:

```python
profile.ndim == 2
profile.shape[1] == 512
np.isfinite(profile).all()
profile.shape[0] >= min_num_traces
```

Если профиль имеет форму `[512, num_traces]`, интерфейс должен предложить транспонировать его.

### 5.3. Статистика профиля

Для каждого профиля рассчитывать:

- число трасс;
- число отсчётов в трассе;
- min;
- max;
- mean;
- std;
- median;
- percentile 1/99;
- наличие NaN;
- наличие inf.

### 5.4. Ожидаемый результат этапа

Пользователь может выбрать профиль, нажать `Load as Clean` или `Load as Real Noisy`, увидеть статистику и отрисовку профиля в главном окне. Если профиль не подходит, пользователь получает понятное сообщение с причиной: неправильная размерность, не 512 отсчётов, NaN, inf или слишком мало трасс.

---

## 6. Этап 3. Предобработка и нормализация

### 6.1. Задачи этапа

1. Реализовать единый pipeline нормализации.
2. Поддержать несколько режимов нормализации.
3. Сохранять параметры нормализации в конфиге эксперимента.
4. Обеспечить обратное преобразование результата в исходный масштаб.

### 6.2. Режимы нормализации

#### 6.2.1. Standard normalization

```python
X_norm = (X - mean) / (std + eps)
```

#### 6.2.2. Robust normalization

```python
X_norm = (X - median) / (mad + eps)
```

#### 6.2.3. Percentile clipping + standardization

```python
X_clip = np.clip(X, p01, p99)
X_norm = (X_clip - mean) / (std + eps)
```

### 6.3. Дополнительные каналы

На этом этапе можно подготовить функции, но не обязательно включать их в первую модель:

- envelope через Hilbert transform;
- горизонтальный градиент;
- вертикальный градиент;
- локальный RMS;
- локальная энергия.

### 6.4. Ожидаемый результат этапа

Профиль можно привести к нормализованному виду и вернуть обратно в исходный масштаб без существенных искажений. Все параметры нормализации сохраняются в конфигурации эксперимента. В интерфейсе пользователь может включать или отключать режимы предобработки и видеть статистику до/после нормализации.

---

## 7. Этап 4. Аналитический генератор синтетических помех

### 7.1. Задачи этапа

1. Реализовать генерацию гиперболических событий.
2. Реализовать генерацию наклонных событий.
3. Реализовать ringing.
4. Реализовать вертикальные всплески.
5. Реализовать широкие шумовые зоны.
6. Реализовать маску помехи.
7. Реализовать смешивание до заданного SNR.

### 7.2. Выход генератора

Генератор должен возвращать:

```python
noisy, clutter, mask, meta
```

Где:

- `noisy` — зашумлённый профиль;
- `clutter` — добавленная помеха;
- `mask` — область влияния помехи;
- `meta` — параметры всех сгенерированных объектов.

### 7.3. Гиперболы

Гипербола задаётся параметрами:

- `x0`;
- `z0`;
- `curvature`;
- `width_x`;
- `amplitude`;
- `frequency`;
- `phase`;
- `polarity`;
- `decay`.

Импульс можно задавать wavelet-функцией, например Ricker wavelet.

### 7.4. Наклонные события

Линия задаётся:

- `x_start`;
- `x_end`;
- `z_start`;
- `slope`;
- `amplitude`;
- `thickness`;
- `frequency`.

### 7.5. Ringing

Ringing задаётся:

- начальной трассой;
- конечной трассой;
- начальным sample;
- частотой;
- затуханием;
- фазой;
- амплитудой.

### 7.6. Вертикальные всплески

Всплески задаются:

- набором трасс;
- шириной по горизонтали;
- типом профиля всплеска;
- амплитудой;
- вероятностью появления.

### 7.7. Ожидаемый результат этапа

Пользователь может выбрать чистый профиль, включить один или несколько типов аналитических помех, нажать `Generate Synthetic Clutter` и увидеть в `MainWindow` чистый профиль, добавленную помеху, маску и итоговый `noisy`-профиль. Для каждого запуска сохраняется `meta`, позволяющий воспроизвести результат при том же `seed`.

---

## 8. Этап 5. Загрузка реальных радарограмм с шумом как паттернов

### 8.1. Назначение этапа

Этот этап нужен для уменьшения главного риска эксперимента: синтетические помехи могут быть слишком непохожи на реальные. Поэтому в блок добавляется возможность загружать реальные радарограммы с воздушными помехами и использовать их как источник шумовых паттернов.

### 8.2. Задачи этапа

1. Добавить загрузку одного или нескольких `real noisy` профилей.
2. Добавить карточку каждого шумового профиля в библиотеку паттернов.
3. Реализовать интерактивное или полуавтоматическое выделение областей шума.
4. Реализовать извлечение patch-паттернов шума.
5. Реализовать нормализацию и каталогизацию паттернов.
6. Реализовать предпросмотр паттернов.
7. Реализовать сохранение и загрузку библиотеки паттернов.

### 8.3. Способы выделения паттернов

#### 8.3.1. Ручное выделение области

Пользователь визуально выбирает область на зашумлённой радарограмме:

```text
x_start, x_end, z_start, z_end
```

Эта область сохраняется как кандидат на шумовой паттерн.

Преимущество:

- максимальный контроль эксперта.

Недостаток:

- требует ручной работы.

#### 8.3.2. Полуавтоматическое выделение по энергии

Автоматически ищутся области с высокой локальной энергией:

```python
local_energy = moving_rms(profile)
candidate_mask = local_energy > threshold
```

Пользователь подтверждает или отклоняет найденные области.

#### 8.3.3. Выделение по остаточной компоненте

Если есть грубый фильтр фона, можно получить остаток:

```python
residual = profile - smooth_background(profile)
```

Из остатка выделяются высокоэнергетические структурные события.

#### 8.3.4. Импорт готовой маски

Если пользователь заранее подготовил маску шума, блок должен позволить загрузить её:

```text
profile.npy + mask.npy
```

### 8.4. Структура паттерна

Каждый паттерн должен хранить:

```python
{
    "pattern_id": "...",
    "source_profile": "...",
    "array": np.ndarray,
    "mask": np.ndarray,
    "bbox": [x_start, x_end, z_start, z_end],
    "normalization": {...},
    "stats": {...},
    "tags": ["pole", "powerline", "ringing"],
    "created_at": "...",
    "comment": "..."
}
```

### 8.5. Теги паттернов

Пользователь должен иметь возможность помечать паттерны:

- `pole`;
- `powerline`;
- `metal_fence`;
- `building`;
- `ringing`;
- `vertical_spike`;
- `unknown`.

Теги пригодятся для управляемой генерации датасета.

### 8.6. Ожидаемый результат этапа

Пользователь может загрузить реальную зашумлённую радарограмму, выделить на ней область воздушной помехи, сохранить эту область как шумовой паттерн, увидеть паттерн отдельно в `MainWindow` и добавить его в библиотеку. Библиотека паттернов сохраняется на диск и может быть повторно загружена в другом эксперименте.

---

## 9. Этап 6. Генератор на основе реальных паттернов
### 9.1. Задачи этапа

1. Реализовать выбор паттерна из библиотеки.
2. Реализовать случайный crop/resize паттерна.
3. Реализовать масштабирование амплитуды паттерна.
4. Реализовать случайный сдвиг по трассам и sample.
5. Реализовать деформации паттерна.
6. Реализовать наложение паттерна на чистый профиль.
7. Реализовать смешанный режим `Synthetic + Pattern`.

### 9.2. Трансформации паттернов

Чтобы один реальный паттерн не копировался слишком буквально, нужны аугментации:

- amplitude scaling;
- polarity flip;
- horizontal flip;
- time shift;
- trace shift;
- mild stretching по горизонтали;
- mild stretching по вертикали;
- random crop;
- random fade-in/fade-out;
- добавление слабого jitter;
- смешивание нескольких паттернов.

### 9.3. Наложение паттерна

Базовая формула для реальных noise-паттернов больше не является аддитивной. Clean и noise рассматриваются как амплитудные изображения в одной шкале `0..256`, поэтому real-pattern extraction по умолчанию должен сохранять raw amplitude. В UI должен быть явный выбор режима наложения: `dominant_amplitude` или `additive`.

Для режима `dominant_amplitude` размещённый паттерн накладывается на той же глубине, на которой он был извлечён: `z_start` берётся из `pattern.bbox` и только ограничивается пределами целевого clean-профиля. Формула:

```python
N_placed = place(pattern, x0, pattern.bbox.z_start)
M_noisy = place(pattern_mask, x0, z0)

X_clean_centered = X_clean - 128
N_centered = N_placed - 128
noise_wins = M_noisy > 0 and abs(N_centered) > abs(X_clean_centered)
X_noisy = where(noise_wins, N_placed, X_clean)
```

Такой режим означает, что шум не складывается с чистым сигналом, а перекрывает его только там, где модуль амплитуды шума больше модуля чистого сигнала. Legacy-режим `additive` можно оставить для сравнения, но он должен быть явно выбран пользователем. Если нужен residual для диагностики, он вычисляется постфактум:

```python
N_effective = X_noisy - X_clean
```

### 9.4. Масштабирование под target SNR

Для real-pattern режима `target_snr_db` масштабирует подготовленный noise-паттерн до этапа амплитудного перекрытия. После перекрытия фактический residual может отличаться от исходного паттерна, поэтому отчётный SNR нужно считать по эффективной разнице:

```python
beta = compute_beta_for_target_snr(X_clean_patch, pattern, target_snr_db)
X_noisy = dominant_amplitude_overlay(X_clean, beta * pattern, mask)
N_effective = X_noisy - X_clean
actual_snr_db = compute_snr_db(X_clean, N_effective)
```

### 9.5. Смешанный режим

В смешанном режиме компоненты сначала собираются в общий noise-профиль, а итоговый `X_noisy` формируется тем же амплитудным перекрытием:

```python
N_total = beta_1 * N_synthetic + beta_2 * N_pattern
X_noisy = dominant_amplitude_overlay(X_clean, N_total, M_total)
N_effective = X_noisy - X_clean
```

Должны сохраняться обе компоненты:

```python
meta["synthetic_clutter"]
meta["pattern_clutter"]
meta["total_clutter"]
```

### 9.6. Ожидаемый результат этапа

Пользователь может выбрать clean-профиль, выбрать один или несколько реальных шумовых паттернов, настроить силу и явный режим наложения (`dominant_amplitude`/`additive`), сгенерировать зашумлённый профиль и увидеть в `MainWindow`: clean, выбранный паттерн в исходной амплитудной шкале, размещённый паттерн на исходной глубине, effective clutter, noisy и dominance/effective mask. Генератор умеет работать как только с реальными паттернами, так и в смешанном режиме с аналитической синтетикой. Основная обучающая пара — `noisy -> clean`.

---

## 10. Этап 7. Создание обучающего датасета по упрощённой схеме

### 10.1. Принятое ограничение этапа

На текущем этапе отказываемся от генерации обучающих пар через аналитические помехи, real-pattern overlay и mixed generator как от обязательной части dataset pipeline. Базовая структура датасета временно строится только на готовых парных данных:

```text
X_clean          — чистый сигнал;
X_noisy_same     — этот же сигнал после добавления/наличия шума;
training pair    — input: X_noisy_same, target: X_clean.
```

Ключевое требование: `clean` и `noisy` должны описывать один и тот же профиль, иметь одинаковую геометрию, одинаковый порядок трасс и одинаковую шкалу амплитуд. `clutter`, `mask`, `pattern_id`, synthetic/meta generation пока не являются обязательными полями датасета.

### 10.2. Задачи этапа

1. Реализовать загрузку пар `clean/noisy` как основной источник данных.
2. Добавить явную проверку соответствия пары: shape, число трасс, число samples, отсутствие `NaN/inf`, совместимый диапазон амплитуд.
3. Поддержать несколько независимых пар `clean/noisy`.
4. Нарезать каждую пару на согласованные patch’и: один и тот же `x_start/x_end` применяется к `clean` и `noisy`.
5. Разделить patch’и на train/validation/test без утечки соседних фрагментов.
6. Сохранить `dataset_summary.json` с перечнем исходных пар, параметрами нарезки и статистикой по split.
7. Оставить поля для диагностического residual/mask как вычисляемые optional-артефакты, но не требовать их для обучения.

### 10.3. UI-изменения вкладки `Dataset`

Вкладка `Dataset` должна временно фокусироваться на парных профилях:

- кнопка `Add Clean/Noisy Pair`;
- таблица загруженных пар;
- статус валидации каждой пары;
- кнопка `Preview Pair`;
- настройки `patch width`, `stride`, `split train/validation/test`;
- кнопка `Build Dataset`;
- кнопка `Preview Random Patch`;
- кнопка `Save Dataset`;
- кнопка `Load Dataset`.

Поля, связанные с количеством synthetic variants, паттернами, тегами и режимом генератора, можно оставить скрытыми/disabled до возврата к расширенной схеме.

### 10.4. Валидация пары

Минимальные проверки для каждой пары:

```python
clean.ndim == 2
noisy.ndim == 2
clean.shape == noisy.shape
clean.shape[1] == 512
np.isfinite(clean).all()
np.isfinite(noisy).all()
clean.shape[0] >= min_num_traces
```

Дополнительные предупреждения:

- заметно разные min/max или percentile 1/99 у `clean` и `noisy`;
- слишком маленькая разница `noisy - clean`, из-за которой пара почти не содержит шума;
- слишком большая разница, похожая на несовпадающие профили;
- подозрение на транспонированный массив `[512, num_traces]`.

### 10.5. Структура sample

Каждый sample должен содержать минимальную supervised-пару:

```python
{
    "noisy": noisy_patch,
    "clean": clean_patch,
    "pair_id": "...",
    "source_clean_profile": "...",
    "source_noisy_profile": "...",
    "x_start": int,
    "x_end": int,
    "normalization": {...},
    "sample_meta": {...}
}
```

Optional diagnostic-поля могут вычисляться при сборке или предпросмотре, но не являются обязательными для модели:

```python
{
    "residual": noisy_patch - clean_patch,
    "difference_stats": {...}
}
```

### 10.6. Split-стратегия

Главная цель — не допустить утечку почти одинаковых соседних patch’ей между train и validation/test.

Рекомендуемый порядок стратегий:

1. Если пар несколько — split по `pair_id`: часть пар полностью уходит в validation/test.
2. Если пара одна — split по крупным непрерывным диапазонам трасс, а не по случайным patch’ам.
3. Между split-зонами оставлять gap не меньше одного `patch_width`, чтобы соседние перекрывающиеся patch’и не попали в разные split.
4. Test-set фиксировать и не менять между запусками при одинаковом seed/config.

### 10.7. Dataset summary

`dataset_summary.json` должен содержать:

```json
{
  "dataset_schema": "paired_clean_noisy_v1",
  "pairs": [
    {
      "pair_id": "...",
      "clean_path": "...",
      "noisy_path": "...",
      "shape": [0, 512],
      "clean_stats": {},
      "noisy_stats": {},
      "difference_stats": {}
    }
  ],
  "patch_width": 0,
  "stride": 0,
  "split_strategy": "by_pair_or_trace_blocks",
  "num_train_patches": 0,
  "num_validation_patches": 0,
  "num_test_patches": 0,
  "normalization": {}
}
```

### 10.8. Ожидаемый результат этапа

Пользователь может добавить одну или несколько пар `clean/noisy`, увидеть результат валидации, собрать patch dataset и сохранить его на диск. Датасет содержит только то, что нужно для первого baseline-обучения: `input = noisy`, `target = clean`. Residual `noisy - clean` используется только для диагностики, визуализации и метрик, но не как обязательный target.

---

## 11. Этап 8. Baseline-модель очистки для парного датасета

### 11.1. Задачи этапа

1. Реализовать простую CNN-модель как baseline.
2. Реализовать Small U-Net как второй baseline.
3. Основным режимом сделать прямое восстановление `clean` из `noisy`.
4. Поддержать входные каналы `raw`; дополнительные каналы `envelope`, `grad_x`, `grad_z` оставить опциональными.
5. Поддержать сохранение и загрузку модели.
6. Зафиксировать, что residual/clutter-режимы пока не входят в обязательный MVP.

### 11.2. Рекомендуемый первый режим

Первым реализуется supervised direct-clean режим:

```text
input:  X_noisy_same
target: X_clean
output: X_clean_pred
```

Причина: при упрощённой схеме у нас есть достоверная парная разметка `clean/noisy` одного и того же сигнала, поэтому модель можно обучать напрямую восстанавливать чистый профиль без предположений о физической модели шума и без маски помехи.

### 11.3. Ожидаемый результат этапа

В интерфейсе можно выбрать baseline-модель очистки, указать число входных каналов, создать модель, увидеть число параметров и сохранить не обученный checkpoint. Модель принимает patch `[channels, width, 512]` и возвращает очищенный профиль `[1, width, 512]`.

---

## 12. Этап 9. Обучение модели

### 12.1. Задачи этапа

1. Реализовать train loop для sample’ов `noisy -> clean`.
2. Реализовать validation loop.
3. Реализовать checkpoint best/last.
4. Реализовать раннюю остановку.
5. Реализовать отправку progress в UI.
6. Реализовать preview validation patch во время обучения: `clean`, `noisy`, `clean_pred`, `error`.

### 12.2. Loss-функции

Базовый loss:

```python
L_clean = L1(clean_pred, clean_true)
```

Дополнительный gradient loss:

```python
L_grad = L1(grad(clean_pred), grad(clean_true))
```

Итоговый loss для MVP:

```text
L = L_clean + λ_grad * L_grad
```

Residual loss, mask loss и регуляризация избыточного вычитания не требуются для первого обучения по парному датасету. Их можно вернуть позже, когда снова появятся генераторные `clutter/mask` targets.

### 12.3. Потоки

Обучение должно выполняться не в UI-потоке. Нужно использовать `QThread`, `QRunnable` или отдельный процесс. UI получает только сигналы прогресса:

```python
epoch_finished(metrics)
preview_ready(arrays)
log_message(text)
training_finished(summary)
training_error(message)
```

### 12.4. Ожидаемый результат этапа

Пользователь может нажать `Start Training`, обучение запускается без зависания интерфейса, в `MainWindow` обновляются графики loss и validation preview, а в конце сохраняется лучший checkpoint. В результатах отображаются ошибки восстановления чистого сигнала на validation/test split парного датасета.

---

## 13. Этап 10. Метрики качества

### 13.1. Задачи этапа

1. Реализовать метрики для пар `clean/noisy`, где `clean` является ground truth.
2. Реализовать сравнение `noisy` против `clean` и `clean_pred` против `clean`.
3. Реализовать отчёт по эксперименту.
4. Оставить косвенные метрики для реальных профилей без пары как дополнительный режим инференса.

### 13.2. Метрики для парного датасета

- MAE до очистки: `MAE(noisy, clean)`;
- MAE после очистки: `MAE(clean_pred, clean)`;
- RMSE до очистки;
- RMSE после очистки;
- SNR before;
- SNR after;
- SNR gain;
- PSNR before/after, если амплитудная шкала фиксирована;
- SSIM или структурная корреляция, если будет полезно для визуального качества;
- residual energy: `clean_pred - noisy` как оценка величины внесённых изменений.

### 13.3. Защита от избыточной очистки

Так как маски помехи пока нет, риск удаления полезных отражений оценивается косвенно:

- ошибка `clean_pred - clean` в областях с высокой энергией `clean`;
- корреляция трасс `clean_pred` и `clean`;
- сравнение спектра/градиентов до и после очистки;
- визуальный preview нескольких validation/test patch’ей.

### 13.4. Метрики без ground truth

Для одиночного реального noisy-профиля без paired clean остаются только косвенные показатели:

- доля изменённой энергии;
- локальный RMS до/после;
- корреляция original/cleaned;
- средняя амплитуда residual;
- карта зон сильного изменения;
- экспертная отметка качества.

### 13.5. Ожидаемый результат этапа

После обучения пользователь видит таблицу метрик, где явно сравнивается качество до и после очистки относительно paired `clean` ground truth. Отчёт должен показывать, улучшилась ли ошибка восстановления чистого сигнала и нет ли признаков чрезмерного сглаживания или удаления полезных отражений.

---

## 14. Этап 11. Инференс на полном профиле

### 14.1. Задачи этапа

1. Реализовать sliding-window inference для модели `noisy -> clean_pred`.
2. Реализовать усреднение перекрывающихся patch’ей.
3. Реализовать просмотр `noisy`, `clean_pred`, `residual = noisy - clean_pred`.
4. Реализовать alpha-слайдер как мягкое смешивание исходного и предсказанного профиля.
5. Реализовать сохранение результата.

### 14.2. Sliding-window схема

```python
for x_start in range(0, num_traces - patch_width + 1, stride):
    patch = X_noisy[x_start:x_start + patch_width]
    pred_clean = model(patch)
    output_sum[x_start:x_end] += pred_clean * window
    weight_sum[x_start:x_end] += window

clean_pred_full = output_sum / weight_sum
```

### 14.3. Финальная очистка

Так как модель предсказывает clean напрямую, базовый результат:

```python
cleaned = clean_pred_full
```

Для ручного управления силой очистки используется смешивание:

```python
cleaned = (1 - alpha) * noisy + alpha * clean_pred_full
```

где `alpha = 0` оставляет исходный noisy-профиль, а `alpha = 1` полностью использует предсказание модели.

### 14.4. Ожидаемый результат этапа

Пользователь может выбрать модель и профиль, нажать `Run Inference`, увидеть `noisy`, `clean_pred`, `cleaned` и `residual` в `MainWindow`, изменить `alpha` и сразу увидеть более мягкую или более сильную очистку. Результат можно сохранить как новый профиль или как отдельный артефакт эксперимента.

---

## 15. Этап 12. Визуализация всех преобразований

### 15.1. Задачи этапа

1. Реализовать единый API визуализации.
2. Поддержать отображение paired `clean/noisy` данных.
3. Поддержать сравнение трасс.
4. Поддержать графики обучения и метрик.
5. Поддержать текстовый лог эксперимента.

### 15.2. Режимы отображения радарограммы

Обязательные для упрощённой схемы:

- `Clean`;
- `Noisy`;
- `Predicted clean`;
- `Cleaned with alpha`;
- `Residual noisy-predicted`;
- `Error map predicted-clean`, если есть paired ground truth.

Дополнительные режимы, которые можно оставить для будущего возврата к генераторной схеме:

- `Extracted pattern`;
- `Synthetic clutter`;
- `Pattern clutter`;
- `Mixed clutter`;
- `True mask`;
- `Predicted mask`.

### 15.3. Режимы отображения трассы

Для выбранной трассы показывать:

- clean, если доступен;
- noisy;
- predicted clean;
- cleaned with alpha;
- residual;
- error относительно clean, если доступен.

### 15.4. Ожидаемый результат этапа

Все ключевые преобразования парного pipeline видны в основном окне приложения: загрузка пары, нарезка patch’ей, обучение, validation preview и инференс. Пользователь работает не с абстрактным clutter target, а с понятной схемой `noisy -> clean`.

---

## 16. Этап 13. Сохранение и воспроизводимость эксперимента

### 16.1. Задачи этапа

1. Сохранять полный конфиг эксперимента.
2. Сохранять seed.
3. Сохранять список `clean/noisy` пар.
4. Сохранять train/validation/test split.
5. Сохранять checkpoints.
6. Сохранять метрики и preview.
7. Генерировать markdown-отчёт.

### 16.2. Минимальный набор файлов

```text
config.json
dataset_summary.json
split.json
train_log.csv
metrics.json
model_best.pt
report.md
```

`pattern_library_index.json` для упрощённой схемы не обязателен и нужен только при возврате к real-pattern generator.

### 16.3. Ожидаемый результат этапа

Любой запуск эксперимента можно открыть позже, посмотреть параметры, восстановить paired dataset, загрузить модель и повторить инференс. Это особенно важно для сравнения разных наборов пар `clean/noisy`, разных split-стратегий и разных baseline-моделей.

---

## 17. Этап 14. Проверка на отложенных парных профилях

### 17.1. Задачи этапа

1. Обучить модель на train split парного датасета.
2. Подобрать параметры по validation split.
3. Один раз оценить финальное качество на test split.
4. Сравнить несколько baseline-моделей на одинаковых split.
5. Оценить визуально, насколько бережно сохраняются полезные отражения.

### 17.2. Сравниваемые варианты

```text
A. Simple CNN, raw channel
B. Small U-Net, raw channel
C. Small U-Net, raw + optional derived channels
D. Small U-Net + alpha tuning at inference
```

Сравнения synthetic-only, pattern-only и mixed generator временно откладываются до возврата к расширенной схеме генерации.

### 17.3. Ожидаемый результат этапа

Должна появиться понятная сравнительная таблица по отложенным paired test-профилям: какая модель лучше восстанавливает `clean`, насколько снижается MAE/RMSE, насколько растёт SNR и нет ли визуальных признаков удаления полезных подповерхностных отражений.

---

## 18. Этап 15. Минимальный MVP-план внедрения

### 18.1. MVP-1: UI и загрузка пар

Результат:

- есть кнопка `ML Clutter`;
- открывается окно эксперимента;
- можно добавить пару `clean/noisy`;
- пара проходит валидацию;
- можно увидеть `clean`, `noisy` и `noisy-clean` preview.

### 18.2. MVP-2: парный датасет

Результат:

- можно собрать patch dataset из одной или нескольких пар;
- patch dataset содержит обязательные поля `clean` и `noisy`;
- dataset summary сохраняется на диск;
- можно просмотреть случайный patch.

### 18.3. MVP-3: обучение baseline

Результат:

- можно обучить direct-clean CNN или Small U-Net;
- обучение не блокирует UI;
- видны train/validation loss;
- сохраняется best checkpoint;
- validation preview отображает `noisy`, `clean`, `predicted clean`, `error`.

### 18.4. MVP-4: инференс

Результат:

- можно применить модель к полному noisy-профилю;
- можно регулировать силу очистки `alpha` через смешивание с исходным сигналом;
- отображаются `clean_pred`, `cleaned` и `residual`;
- результат можно сохранить.

### 18.5. Отложено за пределы текущего MVP

- обязательная библиотека шумовых паттернов;
- synthetic/pattern/mixed generator как источник train data;
- `clutter` и `mask` как обязательные target;
- residual-модель, предсказывающая удаляемую компоненту;
- сравнение synthetic-only / pattern-only / mixed.

---

## 19. Риски и меры снижения

### 19.1. Риск: несоответствие clean/noisy пары

Описание: `clean` и `noisy` могут быть разными профилями, иметь сдвиг по трассам, отличаться масштабом или быть транспонированы.

Меры:

- строгая проверка `shape`;
- preview пары и difference map;
- статистика `noisy-clean`;
- предупреждения при аномально большой/малой разнице;
- ручное подтверждение сомнительных пар.

### 19.2. Риск: малый объём парных данных

Описание: модель может переобучиться на одну-две пары.

Меры:

- split по крупным блокам трасс с gap;
- простые аугментации, не нарушающие парность: flip по трассам, amplitude scaling обеих частей пары, crop;
- ранняя остановка;
- фиксированный test split;
- сбор дополнительных пар.

### 19.3. Риск: модель удаляет полезные объекты

Описание: модель может сгладить реальные подповерхностные отражения вместе с шумом.

Меры:

- direct-clean validation против ground truth;
- gradient loss;
- метрики ошибки в высокоэнергетических областях clean;
- alpha-слайдер;
- визуализация residual и error map;
- ручной контроль оператора.

### 19.4. Риск: UI зависает при обучении

Меры:

- обучение в worker thread/process;
- progress signals;
- кнопка stop;
- сохранение промежуточных checkpoints.

### 19.5. Риск: нерепродуцируемые эксперименты

Меры:

- сохранение seed;
- сохранение config;
- сохранение списка пар;
- сохранение split;
- сохранение train log.

---

## 20. Итоговый ожидаемый результат разработки

После реализации упрощённого этапа разработки экспериментальный блок `ML Clutter` должен позволять провести первый полный supervised-цикл без обязательной генерации синтетики и паттернов:

1. Открыть блок по кнопке `ML Clutter`.
2. Добавить одну или несколько пар `clean/noisy`, где `noisy` — тот же сигнал с шумом.
3. Проверить геометрию, статистику и визуальное соответствие пары.
4. Собрать patch dataset по схеме `input: noisy`, `target: clean`.
5. Обучить direct-clean baseline-модель.
6. Оценить качество на validation/test split по ground truth `clean`.
7. Применить модель к полному noisy-профилю.
8. Визуально и численно оценить результат.
9. Сохранить модель, очищенный профиль, конфиги, split, метрики и отчёт.

Главный критерий успеха текущего упрощённого MVP:

```text
Модель, обученная на парных данных clean/noisy одного и того же сигнала,
должна уменьшать ошибку относительно clean ground truth на отложенных patch’ах
и при этом визуально сохранять полезные подповерхностные отражения.
```

Расширенная схема с real-pattern library, synthetic generator, mixed generator, clutter/mask targets и сравнением synthetic-to-real остаётся зафиксированной в ранних этапах плана, но возвращается в работу только после стабилизации базового парного pipeline.
